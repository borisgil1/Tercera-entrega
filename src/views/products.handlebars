<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Inicio</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f3e5f5;
            color: #4a148c;
        }

    .navbar {
        background-color: #6a1b9a;
        padding: 5px;
        /* Adjusting the padding to make the navbar smaller */
    }
</style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="#">
            <img src="img/logo.PNG" alt="Logo" width="180" height="80" class="d-inline-block align-top">
        </a>
        <div class="navbar-center">Baris Gamer</div>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/products">Tienda</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profile">Mi Perfil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/chat">Chat</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Contáctanos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/carts/666fce3d59d6103fa3c85a11">Carrito</a>
                </li>
            </ul>
        </div>
    </nav>

<p>Bienvenido, {{user.Nombre}} {{user.Apellido}}!</p>
<p>Rol: {{user.Rol}}</p>
<p>Carrito: {{user.Carrito}} </p>

<div class="container">
    <h1 class="mt-4 mb-3">Productos</h1>
    <a href="/carts/{{cartId}}" class="ms-5"></a>
    <div class="row">
        {{#each products}}
        <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100">
                <img class="card-img-top" src="{{this.img}}" alt="">
                <div class="card-body">
                    <h4 class="card-title">{{this.title}}</h4>
                    <h5>${{this.price}}</h5>
                    <h5>${{this._id}}</h5>
                    <p class="card-text">{{this.description}}</p>
                       <form action="/api/carts/{{../user.Carrito}}/products/{{this._id}}" method="POST">
                    <button class="btnAddtoCart" onclick="addToCart('{{this._id}}')"
                        data-id="{{this._id}}">Agregar
                        al carrito</button>
                </form>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>

{{#if hasPrevPage}}
<a href="/products?page={{prevPage}}">Anterior</a>
{{/if}}

{{#if hasNextPage}}
<a href="/products?page={{nextPage}}">Siguiente</a>
{{/if}}

<p>Página {{currentPage}} de {{totalPages}}</p>
</body>
</html>
